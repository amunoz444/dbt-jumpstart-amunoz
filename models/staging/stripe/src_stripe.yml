version: 2

sources:
    - name: stripe
      description: blah blah
      database: raw
      tables:
        - name: payment
          columns:
            - name: id
              tests:
                - unique
                - not_null

    # - name: stg_orders
    #   description: order stuff
    #   columns:
    #     - name: order_id
    #       description: pk
    #       tests:
    #         - unique
    #         - not_null
    #     - name: status
    #       description: "{{ doc('order_status')}}"
    #       tests:
    #         - accepted_values:
    #             values:
    #                 - completed
    #                 - shipped
    #                 - returned
    #                 - placed
    #                 - return_pending
    #     - name: customer_id
    #       tests:
    #         - relationships:
    #             to: ref('stg_customers')
    #             field: customer_id

